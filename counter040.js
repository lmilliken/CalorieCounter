document.getElementById('calorie-form').onsubmit = calculate;

/*
Create the `calculate` function to hold the code to sum up the user's calorie inputs. This function will take one parameter, named `event`.

<I don't know how to explain why this takes an event argument...>

Here is an example of an empty function called `square` that takes a `number` as a parameter
function functionName(number) {
}
*/

console.log(document.getElementById('calorie-form').getOwnPropertyNames());

function calculate(event) {}
